---
name: mingyuan-xuanchuan
description: 明源场宣图片批量规范化处理：生成/审计素材、2K 横竖规范化、轻度优化、PNG 素材叠加、过程预览与打包交付。
---

# 明源场宣图简易制作

> 版本: 0.2.0
> 作者: Alix（明源云海南）
> 用途: 批量场宣图片规范化处理——格式标准化 + 轻度优化 + 素材叠加 + 过程预览 + 打包交付
> 依赖: Python 3.8+, Pillow, NumPy
> 仓库: https://github.com/Alix2395/mingyuan-venue-image-creator

---

## 一、触发条件

- 用户说"场宣图"、"明源场宣"、"批量处理图片叠加素材"
- 用户发送 zip 压缩包（含 `素材/` `原图/` 两个文件夹）
- 用户要求对图片进行 2K 标准化 + 素材叠加
- 用户要求跑测试流程/演示模式

---

## 二、工作流总览

```
用户提供zip(素材/ + 原图/)
    │
    ▼
[阶段0] 解包 & 素材审计
    │  确认素材数量、透明度、用户使用方式
    │  未说明则主动询问每张素材的放置位置
    │  自动生成：素材审计图 + 原片审计图
    │
    ▼
[阶段1] 格式规范化 ── 2K标准
    │  横板 → 2560×1440
    │  竖板 → 1440×2560
    │  等比智能裁切，居中保留主体
    │  输出: 临时/normalized/横板/ + 临时/normalized/竖版/
    │  自动生成：规范化前后对比图
    │
    ▼
[阶段2] 轻度图片优化
    │  调整曝光 / 白平衡 / 对比度
    │  不做破坏性处理（不锐化、不降噪、不大幅调色）
    │  输出: 临时/enhanced/
    │  自动生成：优化前后对比图
    │
    ▼
[阶段3] 素材叠加 ── 先各1张确认
    │  横板样板 ×1 + 竖板样板 ×1
    │  发给用户确认效果
    │  确认后全量处理
    │  输出: 临时/final/
    │  自动生成：叠加样板图 + 全量总览图
    │
    ▼
[阶段4] 打包交付
    │  横板/竖板分目录 → zip
    │  发送用户，任务完成
    │  日志留存
```

---

## 三、阶段详解

### 测试/演示模式：必须生成过程预览

当用户要求「测试流程」「跑一下流程」「每个步骤汇报」「发过程图片」「要能明显看出各步骤差别」时，进入测试/演示模式：

1. 使用 `工具/generate_test_data.py` 生成覆盖横板/竖板/超宽/超窄/正方等比例的测试原图，以及透明 PNG 素材。
2. 每个阶段必须输出一张过程预览 PNG，并发送给用户：
   - 阶段0：素材审计图 + 原图分辨率审计图
   - 阶段1：规范化前后对比图（标注原始尺寸 → 目标尺寸，突出裁剪/缩放差异）
   - 阶段2：优化前后对比图（同一张图左右对比，突出亮度/对比度/色彩变化）
   - 阶段3：素材叠加样板图（明显看到 Logo/水印/二维码/徽章等位置差异）
   - 阶段4：成品总览图 + zip 包
3. 过程图统一放入 `成果/<任务名>/过程预览/`，最终 zip 放入 `成果/<任务名>/`。
4. 汇报要短：只说当前阶段、输入/输出数量、关键差异、预览图路径；不要长篇解释。

可复用脚本：`工具/create_process_previews.py` 用于生成阶段预览拼图。

### 阶段0：解包 & 素材审计

1. 接收用户发送的 zip 压缩包
2. 解压到临时工作目录
3. 读取 `素材/` 文件夹，列出所有 PNG 素材
4. 读取 `原图/` 文件夹，统计图片数量、检测分辨率
5. **询问用户**：每张素材的放置方式（位置、大小等）
   - 如用户已在消息中说明，直接记录
   - 如未说明，必须主动询问并等待确认

**位置映射门禁：**
- `bottom-center` 必须是底部居中，按 `x=(bg_w-asset_w)/2`、`y=bg_h-asset_h-margin` 计算。
- `top-center` 必须是顶部居中，按 `x=(bg_w-asset_w)/2`、`y=margin` 计算。
- 禁止把 top/bottom center 当成画面中心偏移，否则水印会落在中部。

### 阶段1：格式规范化

**核心脚本：** `工具/normalize_resolution.py`

**规则：**
| 判断条件 | 处理方式 |
|----------|---------|
| 宽>高（横板） | 目标 2560×1440 |
| 高>宽（竖板） | 目标 1440×2560 |
| 原图比例与目标相同或极其近似（偏差<3%） | 直接缩放 |
| 原图比例与目标差异较大 | 等比缩放至短边贴合 → 居中裁剪长边 |

**禁止拉伸变形。**

**输出目录：**
- `临时/normalized/横板/` — 所有横板原图处理后
- `临时/normalized/竖版/` — 所有竖板原图处理后

完成后**必须汇报**用户：横板 X 张、竖板 Y 张已规范化。

### 阶段2：轻度优化

**核心脚本：** `工具/enhance_images.py`

**处理内容（仅轻度调整）：**
- 自动曝光校正（轻微）
- 白平衡微调
- 对比度增强（1.05-1.15 倍）
- 色彩饱和度微提（1.0-1.1 倍）

**禁止项：**
- 不做破坏性滤镜
- 不锐化
- 不降噪
- 不大幅调色
- 不做 AI 内容识别

使用 Pillow `ImageEnhance` 模块，参数保守。

### 阶段3：素材叠加

**核心脚本：** `工具/overlay_assets.py`

**流程：**
1. 先处理横板 1 张 + 竖板 1 张作为样板
2. 发样板给用户确认效果
3. 用户确认后，全量处理所有图片

**关键要求：**
- PNG 素材透明度必须完整保留（RGBA 模式）
- 叠加位置精确按用户指定
- 位置描述映射：
  - "右上角" → 距右边 40px, 距上边 40px
  - "右下角" → 距右边 40px, 距下边 40px
  - "左上角" → 距左边 40px, 距上边 40px
  - "左下角" → 距左边 40px, 距下边 40px
  - 自定义 → 按用户指定的像素/比例

### 阶段4：打包交付

1. 最终产物放入 `成果/` 目录
2. 横板 → `成果/场宣图_横板/`
3. 竖板 → `成果/场宣图_竖板/`
4. 打包 zip：`成果/场宣图_成品_YYYYMMDD.zip`
5. 发送用户
6. 留任务日志

---

## 四、检查清单（逐项验证）

```
□ [阶段0] 素材数量已确认
□ [阶段0] 每张素材使用方式已记录
□ [阶段0] 原图分辨率分布已统计
□ [阶段1] 横板图片全部处理为 2560×1440
□ [阶段1] 竖板图片全部处理为 1440×2560
□ [阶段1] 无拉伸变形（等比裁剪）
□ [阶段2] 所有图片已轻度优化
□ [阶段2] 优化参数保守、无破坏性处理
□ [阶段3] 样板已发给用户确认
□ [阶段3] PNG 透明度未丢失
□ [阶段3] 素材位置正确
□ [阶段4] 横板/竖板分目录
□ [阶段4] zip 打包成功
□ [阶段4] 日志已留存
```

---

## 五、工具脚本说明

| 脚本 | 用途 | 输入 | 输出 |
|------|------|------|------|
| `工具/normalize_resolution.py` | 格式规范化到2K | 原图目录 | 横板/竖板目录 |
| `工具/enhance_images.py` | 轻度图片优化 | 规范化后目录 | 优化后覆盖或新目录 |
| `工具/overlay_assets.py` | PNG素材叠加 | 优化后图片 + 素材 | 最终成品目录 |
| `工具/pipeline.py` | 一键全链路 | zip包 + 配置 | 成果zip |
| `工具/generate_test_data.py` | 生成测试数据 | 参数配置 | 测试用zip |
| `工具/checklist_validator.py` | 检查清单验证 | 产出目录 | 验证报告 |
| `工具/create_process_previews.py` | **过程预览拼图生成器** | 工作目录 | 各阶段对比PNG |

---

## 六、配置文件格式

`config.json`：
```json
{
  "assets": [
    {
      "file": "测试数据/素材/logo.png",
      "position": "top-right",
      "scale": 0.15,
      "margin": 40
    },
    {
      "file": "测试数据/素材/watermark.png",
      "position": "bottom-right",
      "scale": 0.08,
      "margin": 30
    }
  ],
  "output_quality": 95,
  "enhance": {
    "brightness": 1.05,
    "contrast": 1.10,
    "color": 1.05,
    "sharpness": 1.0
  }
}
```

---

## 七、日志留存格式

每次任务完成后在 `日志/` 目录写入：

```markdown
# 任务日志 - YYYY-MM-DD HH:MM

- 原图总数: X 张（横板 M / 竖板 N）
- 素材使用: [素材1 → 右上角, 素材2 → 右下角]
- 处理耗时: X 分钟
- 成品: 成果/场宣图_成品_YYYYMMDD.zip
- 用户反馈: [记录后续修改]
```

---

## 八、仓库与版本

```bash
# 克隆
git clone https://github.com/Alix2395/mingyuan-venue-image-creator.git

# 切换至最新版
git checkout v0.2.0
```

| 版本 | 日期 | 说明 |
|------|------|------|
| v0.2.0 | 2026-05-21 | 过程预览系统、测试数据集、仓库重构 |
| v0.0.1 | 2026-05-15 | 初始版本 |

---

> 版本: 0.2.0 | 作者: Alix（明源云海南） | 更新时间: 2026-05-21
